{% extends 'base.html.twig' %}

{% block title %}FilamentBasecolor{% endblock %}

{% block body %}
    <h1>FilamentBasecolor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ filament_basecolor.id }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ filament_basecolor.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_filament_basecolor_index') }}">back to list</a>

    <a href="{{ path('app_filament_basecolor_edit', {'id': filament_basecolor.id}) }}">edit</a>

    {{ include('filament_basecolor/_delete_form.html.twig') }}
{% endblock %}
