<form method="post" action="{{ path('app_filament_basecolor_delete', {'id': filament_basecolor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ filament_basecolor.id) }}">
    <button class="btn">Delete</button>
</form>
