<header class="bg-white shadow dark:border-gray-600 dark:bg-slate-700">
    <div class="mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-8">
        <h1 class="text-3xl font-bold tracking-tight text-gray-900 dark:text-red-500">{% if target == '' %}Home{% else %}
        {{ target|trans }}{% endif %}</h1>
    </div>
</header>